# A.L.E.C. Neural Engine — Python dependencies
# Install in a venv: pip install -r requirements.txt

# Core inference
llama-cpp-python>=0.2.80        # GGUF model loading; install with Metal:
                                 # CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0

# Training (LoRA fine-tuning)
torch>=2.2.0
transformers>=4.42.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.33.0
trl>=0.9.0

# Database
pyodbc>=5.1.0                   # Azure SQL (requires ODBC Driver 18)

# Excel read/write/export
pandas>=2.2.0
openpyxl>=3.1.0                 # .xlsx support

# Auth
bcrypt>=4.1.0                   # Password hashing (optional, PBKDF2 fallback)

# Utilities
huggingface-hub>=0.23.0         # Model downloads
