#!/usr/bin/env node
/**
 * Thin entrypoint — the real app lives in `backend/server.js` (same as `npm start`).
 * Running `node server.js` from the project root is equivalent to `npm start`.
 */
require('./backend/server.js');
