# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.log
logs/*.log

# Environment files
.env
.env.local
.env.production
.env.backup
.env.bak
.secrets

# IDE
.vscode/
.idea/
*.swp
*.swo

# ── Claude Code / Continue (local to each dev machine) ──────────
.claude/
.continue/
.project_memory.md

# OS
.DS_Store
Thumbs.db

# ── Model files (large binaries — never commit) ─────────────────
data/models/*.bin
data/models/*.gguf
data/models/*.safetensors
data/models/lora/
data/models/checkpoints/
data/models/models--*/
!data/models/.gitkeep

# ── Python ───────────────────────────────────────────────────────
services/neural/.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/

# ── Runtime state (local, not code) ──────────────────────────────
data/dedup/
data/research_seen.json
data/autonomy_log.jsonl
data/drive_log.jsonl
data/memory.db
data/memory.db-*
data/skills.json
data/goals.json
data/local-alec.db
data/query_cache.json

# ── SQLite fallback database ─────────────────────────────────────
data/alec.db
data/alec.db-journal
data/alec.db-wal

# ── Training data (personal / sensitive) ─────────────────────────
data/sft/*.jsonl
data/sft/*.json
data/*.jsonl
data/STOA_TRAINING*.jsonl
data/al-e-c-stoa-training*.jsonl

# Token files (sensitive)
tokens/*.pem

# Data directories
chat/*
history/*
logs/*
data/context/*

# Docker
.docker/

# ── Backups and old versions ─────────────────────────────────────
*.backup
*.bak
backend/server_complete.js
backend/server_complete.js.bak

# ── Nested repo / duplicate folder ──────────────────────────────
A.L.E.C/

# Temporary files
tmp/
temp/
.vercel
.superpowers/
